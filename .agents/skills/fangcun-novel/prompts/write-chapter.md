---
version: 32
changelog: 移除源文参考，避免纯换皮
type: user
phase: write
description: 写章
required_vars: ["N", "新书名", "作者名", "源书名", "目标字数", "目标字数_min", "目标字数_max"]
optional_vars: ["genre", "文笔指纹", "风格类型", "场景运作机制", "本章事件", "全局结构", "改写原则", "角色约束"]
system_prompt: system-generic.md
defaults: {"reasoning_effort": "low", "temperature": 0.8}
---

写《{新书名}》第{N}章。正文第一行写"第{N}章 [章名]"（不加#）。

【plot_guide】projects/{作者名}/{源书名}/rewrites/{新书名}/guides/plot_{N}.md

## 写法指令

{文笔指纹}

## 功能需求

创作指令的「功能需求清单」→ 逐条实现。

**场景自己设计，不可照搬源文或创作指令。**

## 换皮铁律

**场景必须和源文完全不同：**
- 冲突形式不能和源文一样
- 关键场景不能和源文一样
- 对话结构不能和源文一样

## 文体铁律（违反=0分）

**禁止直接写心声** — 用动作/对话/表情表达情绪
**叙述句必须有主语** — 对话可省略，叙述不能省
**用词有上下文支撑** — "本能""下意识"等词必须解释为什么
**角色名必须与角色约束一致** — 不能改名、不能用源文名

## 角色约束（本章出场角色，必须严格遵守）

{角色约束}

**防漂移铁律：**
1. 角色名、称谓、住处必须与上方角色卡完全一致
2. 角色出场时必须符合其行为模式卡片，不可人设崩塌
3. 角色的能力边界必须与角色卡一致，不可跨章波动

## 风格规则（必须执行，不是参考）

### ⚠️ 风格类型铁律（违反=0分）

**你必须严格遵守源文的风格类型，不可偏离：**

{风格类型}

**具体要求：**
- 如果源文是**喜剧/轻喜剧**：你的输出必须有笑点、轻松的氛围、幽默的对话。不能写成悬疑、沉重、暗黑。
- 如果源文是**温馨日常**：你的输出必须有生活气息、温暖的情感、平淡中的幸福。不能写成紧张悬疑、激烈冲突。
- 如果源文是**悬疑/推理**：你的输出必须有悬念、紧张感、推理元素。不能写成轻松搞笑。
- 如果源文是**甜宠**：你的输出必须有甜蜜互动、心动瞬间。不能写成虐心、沉重。

**自检：写完后问自己——"如果读者冲着源文的风格来看这章，会不会觉得走错了片场？"如果会，推翻重写。**

### ⚠️ 场景运作机制（违反=0分）

**你必须严格遵守源文的场景运作机制，不可偏离：**

{场景运作机制}

**这是源文的"底层运作方式"，你必须：**
- 用同样的场景结构（开头方式→推进方式→收尾方式）
- 用同样的节奏曲线（如：紧张→释放、轻松→爆发）
- 用同样的技法（如：误会前置→反转揭示→升级对抗→旁观者吐槽）

**但场景内容、对话、人物必须是全新的！**

**举例：**
- 源文用"误会→反转→升级→吐槽"结构制造笑点
- 你也用"误会→反转→升级→吐槽"结构
- 但误会的内容、反转的方式、吐槽的人物必须完全不同

**自检：写完后问自己——"这个场景的运作方式和源文一样吗？"如果一样但内容不同，✓。如果运作方式也不同，推翻重写。**

## 字数
{目标字数}字（{目标字数_min}~{目标字数_max}）。

## 全局上下文

### 全局结构（本章在全书中的位置）
{全局结构}

### 改写原则（全书统一）
{改写原则}

## 输出路径
projects/{作者名}/{源书名}/rewrites/{新书名}/chapters/ch_{N}.txt

## 输出格式（必须严格遵守）

用XML标签包裹输出，不要输出任何标签外的内容：

```
<chapter>
<title>第{N}章 [章名]</title>
<content>
正文第一段...

正文第二段...
</content>
<wordcount>实际字数</wordcount>
</chapter>
```

## 自查清单（生成后内部校验，不输出）

- [ ] 字数在目标范围内
- [ ] 没有照搬创作指令中的任何示例或引用
- [ ] 场景是自己设计的（不是从源文还原的）
- [ ] 台词是自己写的（不是从源文改几个字的）
- [ ] 角色名全部正确
- [ ] 风格正面指令全部执行
- [ ] 风格反面踩雷全部回避
- [ ] 情绪功能与创作指令一致
- [ ] 场景运作方式与源文一致（结构、节奏、技法）
- [ ] 但场景内容是全新的（不是模仿源文的具体场景）
