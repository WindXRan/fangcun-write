---
version: 20
changelog: 风格分析从"参考"提升为"核心规则"
type: user
phase: write
description: 写章
required_vars: ["N", "新书名", "作者名", "源书名", "目标字数", "目标字数_min", "目标字数_max", "世界观"]
optional_vars: ["genre", "女主名", "男主名", "文笔指纹", "角色行为卡片"]
system_prompt: system-generic.md
defaults: {"reasoning_effort": "low", "temperature": 0.8}
---

写《{新书名}》第{N}章。正文第一行写"第{N}章 [章名]"（不加#）。章名取 plot_guide 中标注的，未标注则自拟。

【plot_guide】projects/{作者名}/{源书名}/rewrites/{新书名}/guides/plot_{N}.md

## 核心规则

**禁止串书** — 严禁使用 plot_guide 以外的任何剧情/角色/世界观。

**字数** — 目标 **{目标字数}字**（{目标字数_min}~{目标字数_max}）。末尾加 `【字数：XXX字】`。

**时间线/地点** — 与 plot_guide 时间锚点一致，不允许倒退。地点必须在世界观设定内。

**反AI** — 禁用：倏地/入目/紧了床单/她想起来了/她回忆道/这时/突然/然后/紧接着/她明白了/她意识到/必须/不得不/一下子/忽然/转眼间/只见/但见。句首多样，不连续2句人名开头。

## 风格规则（必须执行，不是参考）

以下是源文的写法分析。**正面指令必须全部执行，反面踩雷必须全部回避。** 写完后逐条核对。

{文笔指纹}

## 角色约束

{角色行为卡片}

**配角防漂移：**
1. 配角名字、称谓、住处必须与 characters.md 完全一致
2. 配角出场时必须符合其行为卡片，不可人设崩塌
3. 配角的能力边界必须与 characters.md 一致，不可跨章波动

## 角色名
女主={女主名}，男主={男主名}。

## 世界观
{世界观}

## 输出路径
projects/{作者名}/{源书名}/rewrites/{新书名}/chapters/ch_{N}.txt

## 自查清单（生成后内部校验，不输出）

- [ ] 字数在目标范围内（{目标字数_min}~{目标字数_max}）
- [ ] 章名取自 plot_guide 或自拟（不与源文重复）
- [ ] 角色名与 characters.md 一致
- [ ] 时间锚点与 plot_guide 一致，无倒退
- [ ] 无 AI 禁用词（倏地/入目/紧了床单/她想起来了 等）
- [ ] 句首多样，未连续2句人名开头
- [ ] 风格正面指令全部执行（逐条核对）
- [ ] 风格反面踩雷全部回避（逐条核对）
- [ ] 对话标签种类 ≥ 源文数值
- [ ] 情绪反应种类 ≥ 源文数值
- [ ] 具体细节支撑情绪，无抽象描述
