---
version: 13
changelog: Add: 身份锚铁律+时间线连续性铁律+角色状态连贯铁律
type: user
phase: guides
description: 章纲生成
required_vars: ["N", "新书名", "作者名", "源书名", "源文全文", "目标字数", "目标字数_min", "目标字数_max", "源文字数", "女主名", "男主名", "世界观"]
optional_vars: ["genre", "本章事件"]
system_prompt: system-generic.md
defaults: {"reasoning_effort": "low", "temperature": 0.8}
---

为《{新书名}》第{N}章生成写章指南（章纲）。

**核心任务：无脑学源文，不要自己发明风格。**

## 角色（本章出场角色，节拍表里必须用这些名字）

{角色约束}

## 世界观（必须遵守，不可自编时代/背景）
{世界观}

## 本章事件（快速参考，用于校验节拍完整性）
{本章事件}

**禁止：** 禁止文学化描写、禁止抽象描写、禁止过于细腻的描写。要简洁直接。

**铁律1——章名必须与源文完全不同：** 源文章名含任何特有的词汇/意象/结构，新文章名不可使用相同词汇或近义结构。例：源文"爹娘一走，林家悬了"→新文不可写"爹娘一走，XX完了"或"XX走了，XX悬了"。章名表达的意思必须是一个全新的概括。

**铁律2——角色名不得与源文任何角色名重叠：** 仔细检查源文全文中的角色名（包括配角、NPC、甚至只出现一次的人物），新书角色的名字不可与其中任何一个相同或发音相似。写章纲时如发现自己的角色名与源文角色名有重合，必须换掉新书角色名。

**铁律3——时间线必须连续：** 本章的时间点必须在上一章之后。如果上一章是"入府第一天"，本章不能是"已有二宝"——中间必须有合理的过渡。如果源文有时间跳跃，新文可以跟着跳，但必须在节拍表开头用1-2句交代时间流逝（如"入府三个月后""转眼到了秋天"）。

**铁律4——角色状态必须连贯：** 本章开头的角色状态（身份、称谓、住处、已知信息）必须与上一章结尾一致。不可出现"上一章在侯府，这一章在永宁巷"的断裂。

**铁律5——身份锚不可篡改：** 角色身份、称谓、住处必须与 characters.md 中的「身份锚表」一致。如果本章涉及身份变动（如从丫鬟升为姨娘），必须在节拍表中明确标注变动原因和节点，且该变动必须在 plot.md 的规划内。

## 第一步：分析源文

读源文后，输出：
- 风格特征：对话比例、段长、单句段比例、描写风格
- 情绪骨架：情绪基调（喜/暖/甜/虐/燃/悬）、驱动类型（冲突驱动/情感驱动/日常驱动/喜剧驱动）、情绪曲线、爆点或高光位置
- **三大密度评估**：
  - 情绪密度：本章情绪波动频次和强度
  - 信息密度：本章有效信息量（新身份/新底牌/新阴谋/新反转）
  - 情节密度：本章是否有因果锚定、冲突驱动、价值转变

## 单章方法论（从 drama-engine 迁移）

### 情绪表达四通道

根据人物性格和所处环境选择：
1. **行动**：通过人物行为动作传递情绪（撕扯、狂奔、捶打、下意识握拳、颤抖的手）
2. **语言**：痛斥、语无伦次、泣不成声、大吼、嘶哑、无声、结巴
3. **环境**：悲伤/压抑用阴雨天、空街道；紧张/危险用急促脚步、闪烁灯光；甜蜜/温暖用夕阳、暖光
4. **独白**：当情绪无法用行动/语言直接表达时使用

### 先压后爆，制造反差

- 先用反派打压、误解、困境让主角"委屈/隐忍"
- 在关键节点让主角反击，释放压抑情绪
- 压得越狠反弹越爽

### 信息差强化情绪期待

- 读者知道主角不知道 → 读者"心急如焚"
- 主角知道配角不知道 → 读者"期待打脸"
- 主角配角都不知道读者知道 → 读者"心疼又着急"

### 拉扯（把读者情绪当弹簧）

- 压弹簧到谷底（前面把主角往死里压）
- 来回晃弹簧（核心杀招：先给"危机解除"的错误预期，在读者放松瞬间再致命一击）
- 只一压一弹仅算及格，须来回晃 ≥3 次

## 第二步：生成仿写章纲

**铁律：**
1. 严格对标源文风格（对话比例、段长、单句段比例、描写风格）
2. 冲突强度不低于源文（可升级不可降级）
3. 爆点位置相近（±10%），给节奏留弹性
4. 节拍表里的角色名必须用上面指定的名字，不可用源文原名或自编名字
5. **每个节拍至少分配50字**，不要拆成十几个字的超短节拍（过细的拆分会导致写作时无法组织内容）
6. **场景必须重构：** 节拍的"功能定位/情绪"与源文一致，但场景内容（地点、道具、动作、台词）必须重新设计。示例：源文这个节拍是"女主在祠堂跪着哭"，新文不可写成"女主在坟前跪着哭"——换场景不换情绪，不换动作模式。
7. **意象不可重复：** 前几章用过的环境意象（如石榴树、窗外的鸟、井水的光）不可在本章再次使用。每个意象只用一次，用过即弃。如果前几章已经用了"石榴树"，本章换成别的植物或环境元素。

## 输出格式

直接输出完整的章纲 markdown，把 {} 内的占位符替换为实际内容，保持以下模板结构不变：

# 第{N}章 {章名}

## 章纲

## 时间锚点（必须标注，防止时间线混乱）
本章时间点：{如"入府第3天"、"怀孕第5月"、"生子后第2年"}

## 前章状态（写章前必须读，用于前章禁用清单）
- 前章结尾方式：{对话/动作/心理/环境}
- 前章已用意象：{如"石榴树、窗外的鸟"}
- 前章高频词/句式：{如"手指攥紧"出现2次}
- 前章角色状态：{如"孟桐刚入府，住在西院"}

## 本章起始状态
- 角色身份/称谓/住处：{从 characters.md 身份锚表复制}
- 角色能力边界：{从 characters.md 能力区间表复制}

## 源文字数
{源文字数}字 → 新书目标 {目标字数}字（1:1对标）

## 角色（从 settings/characters.md 取，不可改）
- 女主：{女主名}
- 男主：{男主名}

## 叙事策略（仿写必须继承，按源文实际风格标注，非悬疑题材不得强行写"悬念"）
{叙事策略}

## 节拍映射表 ⭐

| # | 结构功能 | 钩子 | 功能定位 | 字数 | 感官 |
|---|---------|------|----------|------|------|
{节拍表}

## 本章高光
{高光}

## 排除项
{排除项}

**重要：你的输出中不得包含源文原文的任何段落或对话。源文仅用于分析节拍和情绪，不得照搬进输出。**

## 地点约束
本章出现的地点：{如"侯府、梨香院、厨房"}（必须在世界观设定内）

【参考设定】projects/{作者名}/{源书名}/rewrites/{新书名}/characters.md
【参考剧情】projects/{作者名}/{源书名}/rewrites/{新书名}/plot.md
【参考世界观】projects/{作者名}/{源书名}/rewrites/{新书名}/world.md

{源文全文}
