# 安全政策

## 报告漏洞

如果您发现项目中存在安全漏洞，请**不要**通过公开的 Issue 报告。

请通过以下方式联系我们：
- 发送邮件至仓库所有者的 GitHub 邮箱
- 通过 GitHub Security Advisories 报告

我们会在收到报告后 48 小时内回复。

## 安全建议

- **不要**将 API Key 提交到代码仓库中
- 使用环境变量或 `.env` 文件存储敏感信息
- `api_key` 设为 `null` 时自动从 `$env:API_KEY` 读取